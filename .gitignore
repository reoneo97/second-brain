# The knowledge vault is its own git repo
notes/
# Private chat-export scratch (16M)
import/
.DS_Store
.claude/settings.local.json

# MCP server venv + python caches
mcp/.venv/
__pycache__/
*.pyc

# MCP secrets (NOTION_TOKEN etc.)
mcp/.env

# MCP tool-call playback log
mcp/logs/
